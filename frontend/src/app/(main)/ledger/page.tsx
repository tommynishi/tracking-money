/** SCR-06 家計簿設定（screen.md）。実装は features/ledger へ委譲する。 */
import { LedgerSettingsScreen } from "@/features/ledger/components/LedgerSettingsScreen";

export default function LedgerPage() {
  return <LedgerSettingsScreen />;
}
