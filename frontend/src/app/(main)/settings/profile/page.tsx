/** SCR-08 アカウント設定（screen.md）。実装は features/auth へ委譲する。 */
import { ProfileScreen } from "@/features/auth/components/ProfileScreen";

export default function ProfilePage() {
  return <ProfileScreen />;
}
